mixin droplist(className, classtext, text1, text2, textb1, textb2, classb1, classb2)
	div(class=className)
		p(class=classtext)= text1
		.buttons
			button(class=classb1)= textb1
			p= text2
			button(class=classb2)= textb2

